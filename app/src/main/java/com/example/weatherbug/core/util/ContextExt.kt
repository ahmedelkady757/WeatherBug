package com.example.weatherbug.core.util

// Context extension utilities — placeholder for future helpers.
// Note: findActivity() was removed because platform dialogs (DatePickerDialog,
// TimePickerDialog) were replaced with Material3 Compose-native equivalents
// which require no Activity context.
